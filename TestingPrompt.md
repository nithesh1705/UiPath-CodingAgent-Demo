Build a test XAML workflow that exercises every business rule and exclusion scenario in the BRD — valid transactions, cancelled/rejected/pending status, out-of-month dates, invalid/missing Employee ID and Customer ID, zero/negative sales amounts, duplicate Transaction IDs, blank/zero targets, achievement % at and around the 80%/100% boundaries, and each performance category threshold — along with every exception scenario (missing file, missing sheet, missing column, locked file, empty file).
The test workflow should run the automation against these scenarios and verify: correct eligibility filtering, correct enrichment, correct Total Sales/Achievement %/commission math, correct performance category and commission eligibility labels, correct exclusion logging with reasons, correct output formatting/sorting/summary sections, and correct handling of each exception scenario.
Run the test workflow, and for any failing case, fix the underlying automation workflow and re-test until every test case passes. Report a final pass/fail summary once complete.
