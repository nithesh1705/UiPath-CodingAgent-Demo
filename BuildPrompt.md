Build a complete UiPath RPA project for the "Sales Performance & Commission Reconciliation Automation." The BRD is not attached — read Sales_Performance_Commission_Automation_BRD.md from the current folder for full requirements. The four input Excel files (Employee_Master.xlsx, Customer_Master.xlsx, Sales_Transactions.xlsx, Target_Master.xlsx) are also in the current folder.
Implement all stages end-to-end: input validation, reading the four master/transaction Excel files, transaction eligibility filtering, data enrichment (employee/customer/target lookups), sales and achievement % calculations, performance classification, commission calculation with the 80% eligibility threshold, and generation of both output reports (Sales_Performance_Report.xlsx and Commission_Report.xlsx).
Apply all formatting rules exactly as specified — header formatting, currency (₹#,##0.00) and percentage (0.00%) formats, conditional formatting on Achievement % and Commission Eligibility, column auto-fit, frozen headers, AutoFilter, and the Region → Branch → Achievement % sort order. Include the management summary section with Top 5/Bottom 5 performers and the excluded-transaction summary.
Maintain an exception/exclusion log covering every exclusion reason listed in the BRD, and implement graceful exception handling for the failure scenarios listed (missing files/sheets/columns, invalid data, duplicates, locked files, empty inputs).
Use a simple, linear sequence-based project structure (a Main.xaml orchestrating modular invoked workflows for each stage) — do not use the REFramework/state-machine template. Use reusable workflow components where practical.
Ensure all files and folders are named clearly and consistently, following proper UiPath project naming conventions (workflows, arguments, folders for Data/Config/Tests/Output, etc.) — no generic or placeholder names.
Use proper, correct UiPath activities for every step (e.g. Excel/Workbook activities for file I/O, Data Table activities for filtering/joining/aggregation, proper exception-handling activities like Try Catch, correct control-flow activities) — no shortcuts via arbitrary code blocks where a standard activity exists. Ensure all generated .xaml files are well-formed, valid UiPath workflow files that open and run correctly in UiPath Studio, with clean activity trees and proper argument/variable definitions. Use your UiPath skills for file/project structure, activity selection, and Excel activity conventions.
